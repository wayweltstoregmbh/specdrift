{
  "name": "specdrift",
  "version": "0.0.1",
  "private": true,
  "description": "Deterministic spec-fidelity verification for AI coding agents: claim-level provenance, enforced verdicts, drift reports.",
  "license": "Apache-2.0",
  "type": "commonjs",
  "bin": {
    "specdrift": "bin/specdrift.js"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node tooling/test.js",
    "lint": "node tooling/lint.js",
    "schemas": "node bin/specdrift.js schema check"
  }
}
